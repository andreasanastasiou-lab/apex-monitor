// Global axios instance.
// Request/response interceptors (Authorization header + 401 auto-logout)
// are registered by AuthContext when the app mounts.
import axios from 'axios'

export default axios
